const adder = function someName(a, b) {
    return a+b;
}
console.log(adder(1, 2));

function adder2 (a, b) {
    return a + b;
}
a = 1;
b = 2;
console.log(`If you add ${a} and ${b} you will get ${adder2(a , b)}.`);